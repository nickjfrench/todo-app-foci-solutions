services:
  todo-api:
    build:
      context: todo-api
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - todo-data:/app/data

volumes:
  todo-data:
