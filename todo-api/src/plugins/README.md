# Plugins Folder

Plugins folder provides a way to define dependencies the whole application needs. Not to be confused with utility functions.

In our usecase, that is for things like db connection pools and HTTP errors, but would support extra functionality like file-based atomicity controls, queues, etc.
